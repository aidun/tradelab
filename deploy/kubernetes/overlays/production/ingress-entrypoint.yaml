apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: tradelab-entrypoint
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares: tradelab-tradelab-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - http:
        paths:
          - path: /tradelab/api
            pathType: Prefix
            backend:
              service:
                name: tradelab-backend
                port:
                  number: 8080
          - path: /tradelab
            pathType: Prefix
            backend:
              service:
                name: tradelab-frontend
                port:
                  number: 3000
